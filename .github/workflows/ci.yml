name: libtester ci
on:
  push: 
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: sudo apt update && sudo apt install git gcc make python3-dev libtool-bin libconfig-dev libc6-dev -y
    - run: sudo ${{ github.workspace }}/build.sh
    - run: sudo remove.sh
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: sudo apt update && sudo apt install git gcc make python3-dev libtool-bin libconfig-dev libc6-dev -y
    - run: sudo ${{ github.workspace }}/build.sh
    - run: make test
    - run: sudo remove.sh